<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
	namespace="com.zmsk.face.mapper.custom.library.CustomerEquipmentLibraryMapper">
	<resultMap id="BaseResultMap" type="com.zmsk.face.pojo.FaceEquipmentLibrary">
		<id column="id" property="id" jdbcType="INTEGER" />
		<result column="library_id" property="libraryId" jdbcType="INTEGER" />
		<result column="equipment_id" property="equipmentId" jdbcType="INTEGER" />
		<result column="sync_status" property="syncStatus" jdbcType="BIT" />
	</resultMap>
	<sql id="Example_Where_Clause">
		<where>
			<foreach collection="oredCriteria" item="criteria" separator="or">
				<if test="criteria.valid">
					<trim prefix="(" suffix=")" prefixOverrides="and">
						<foreach collection="criteria.criteria" item="criterion">
							<choose>
								<when test="criterion.noValue">
									and ${criterion.condition}
								</when>
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
								</when>
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value}
									and
									#{criterion.secondValue}
								</when>
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem"
										open="(" close=")" separator=",">
										#{listItem}
									</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>
	<sql id="Update_By_Example_Where_Clause">
		<where>
			<foreach collection="example.oredCriteria" item="criteria"
				separator="or">
				<if test="criteria.valid">
					<trim prefix="(" suffix=")" prefixOverrides="and">
						<foreach collection="criteria.criteria" item="criterion">
							<choose>
								<when test="criterion.noValue">
									and ${criterion.condition}
								</when>
								<when test="criterion.singleValue">
									and ${criterion.condition} #{criterion.value}
								</when>
								<when test="criterion.betweenValue">
									and ${criterion.condition} #{criterion.value}
									and
									#{criterion.secondValue}
								</when>
								<when test="criterion.listValue">
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem"
										open="(" close=")" separator=",">
										#{listItem}
									</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>
	<sql id="Base_Column_List">
		id, library_id, equipment_id, sync_status
	</sql>

	<select id="queryEquipmentLibraryByLibraryId" resultType="com.zmsk.face.dto.library.EquipmetLibraryDTO">
		SELECT
		equipment_id AS equipmentId,sync_status AS
		syncStatus,face_equipment.remark AS remark FROM
		face_equipment_library,face_equipment WHERE library_id=#{libraryId}
		AND
		face_equipment.id=equipment_id
	</select>

	<select id="queryEquipmentUnSyncFaceLibraryIds" resultType="java.lang.Integer">
		SELECT library_id FROM face_equipment_library WHERE
		equipment_id=#{deviceId}
		AND sync_status=0
	</select>

	<select id="queryEquipmentIdByLibraryId" resultType="java.lang.Integer">
		SELECT
		equipment_id FROM face_equipment_library WHERE
		library_id=#{libraryId}
	</select>
</mapper>