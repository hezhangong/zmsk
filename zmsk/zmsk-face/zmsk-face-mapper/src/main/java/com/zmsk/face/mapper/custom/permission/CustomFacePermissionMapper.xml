<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper
	namespace="com.zmsk.face.mapper.custom.permission.CustomFacePermissionMapper">
	<resultMap id="BaseResultMap"
		type="com.zmsk.face.pojo.FacePermission">
		<id column="permission_id" property="permissionId"
			jdbcType="INTEGER" />
		<result column="pid" property="pid" jdbcType="INTEGER" />
		<result column="name" property="name" jdbcType="VARCHAR" />
		<result column="type" property="type" jdbcType="BIT" />
		<result column="permission_value" property="permissionValue"
			jdbcType="VARCHAR" />
		<result column="uri" property="uri" jdbcType="VARCHAR" />
		<result column="status" property="status" jdbcType="BIT" />
		<result column="ctime" property="ctime" jdbcType="INTEGER" />
		<result column="orders" property="orders" jdbcType="INTEGER" />
	</resultMap>

	<select id="queryPermissionListByUserId" parameterType="integer"
		resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from face_permission
		WHERE
		permission_id IN (
		SELECT
		permission_id
		FROM
		face_role_permission
		WHERE
		role_id IN (
		SELECT
		role_id
		FROM
		face_user_role
		WHERE
		user_id = #{userId}
		)
		)
		ORDER BY
		permission_id
	</select>

	<select id="queryMenuListByUserId" parameterType="integer"
		resultMap="BaseResultMap">
		select
		<include refid="Base_Column_List" />
		from face_permission
		WHERE
		type = 2 AND
		permission_id IN (
		SELECT
		permission_id
		FROM
		face_role_permission
		WHERE
		role_id IN (
		SELECT
		role_id
		FROM
		face_user_role
		WHERE
		user_id = #{userId}
		)
		)
		ORDER BY
		permission_id
	</select>

</mapper>